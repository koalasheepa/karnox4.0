{% stylesheet %} 
.fom-vertical .bulk-section {
  margin-left: 20px;
  letter-spacing:1px;
  font-family: var(--typeHeaderPrimary);
  font-size:25px;
  font-weight:600 !important;
}
 {% endstylesheet %}